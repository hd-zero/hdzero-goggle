{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build firmware",
            "type": "shell",
            "command": "make -C build all",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}